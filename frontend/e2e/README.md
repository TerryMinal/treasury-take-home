# Playwright Test Scaffold

Add these end-to-end specifications once the application can run locally:

- `route-smoke.spec.ts`
- `review-flow.spec.ts`
- `batch-flow.spec.ts`
- `error-handling.spec.ts`

Run these tests against the real local frontend and backend processes, not mocked endpoints, so they validate the MVP workflow end to end.
