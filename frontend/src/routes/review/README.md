# Review Route Test Scaffold

Add `review.test.ts` here once the SvelteKit `/review` route exists.

Expected coverage:

- form rendering
- submission behavior
- loading state
- success state
- structured API error state
- rendering of backend review results without contract translation
