# Batch Route Test Scaffold

Reserve this route area for:

- `batch.test.ts`
- `[jobId]/batch-job.test.ts`

Expected coverage:

- multi-file submission flow
- job creation handling
- polling for batch status
- mixed-success result rendering
- understandable failure messaging for partial item errors
